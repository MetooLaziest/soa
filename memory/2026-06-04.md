

### 庭院三连修复 (12:24-12:40)

**问题1：点击宠物无菜单** → 根因：Game.init 异步加载背景图，sync effect 在 game ready 前提前返回
- 修复：Game.ts 新增 `onReady` 回调 + `_notifyReady()`，App.tsx 新增 `gameReady` state，sync effect 依赖 `[yardPets, gameReady]`
- 移除未使用的 Texture import

**问题2：宠物图片映射** → 根因：addPet 硬编码 monsterType='white'
- 修复：App.tsx 新增 `getMonsterType(modelId)` 映射函数 (1→blue, 2→white, 3→pink)，sync 时传入正确 monsterType
- 服务器 pet PNG 已丢失，用 Python Pillow 重新生成了 pet-blue/pink/idle.png 和 treehouse-bg.jpg 占位图

**问题3：URL 路由交换** (/epet/ → 新项目, 旧项目 → /epet2/)
- 服务器操作：mv dist/epet → dist/epet2 (已丢失，旧文件无备份) → mkdir dist/epet → 部署新构建
- Nginx 新增 `/epet2/` location block
- Vite base 从 `/epet1/` 改为 `./`（相对路径），构建产物可在任意路径下运行
- 同时部署到 dist/epet/ 和 dist/epet1/
- **注意：旧 epet HTML 文件已丢失，epet2/ 目录为空**
