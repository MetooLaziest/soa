# 2026-05-13 Supabase → 自建后端迁移 进度总结

## 当前状态：✅ 前端已成功部署并运行

### 今日完成

**后端增强：**
- 创建 JWT 中间件 `/var/www/iot-ai-doll/backend/src/middleware/auth.js`
- 重写 db routes `/var/www/iot-ai-doll/backend/src/routes/db.js`（支持 JWT Bearer 认证）
- 新增接口：devices、inventory、story/progress、device/mbti、cg/unlock、db/query
- 安装 jsonwebtoken 依赖
- 后端已重启正常运行

**前端部署：**
- 修复 api.ts 第 470 行语法错误（删除 orphaned getEmotionDefinitions 代码）
- npm run build 成功：dist/ (0.49 kB HTML + 65 kB CSS + 577 kB JS)
- 已部署到 `/var/www/iot-ai-doll/frontend/`
- 站点可访问：http://47.98.103.151/

### API 测试通过

| 接口 | 方法 | 状态 |
|------|------|------|
| /api/health | GET | ✅ |
| /api/auth/register | POST | ✅ |
| /api/auth/login | POST | ✅ (返回JWT) |
| /api/auth/me | GET | ✅ |
| /api/db/devices | GET (JWT) | ✅ (返回空数组) |
| /api/db/inventory/:sn | GET (JWT) | ✅ |
| /api/db/story/progress/:sn | GET (JWT) | ✅ |
| /api/db/device/mbti/:sn | GET (JWT) | ✅ |
| /api/db/query | POST (JWT) | ✅ (emotion_definitions/locations/companion_models) |

### 仍有遗留问题

1. **GamePage.tsx 未完全适配**（在本地 `/tmp/iot-project/project/app-97gy5yq4t98h/`）：
   - `supabase.functions.invoke('speech-synthesis')` → 后端返回 `{audio_hex: null}`，TTS 暂时不可用
   - `supabase.functions.invoke('voice-to-text')` → 同上，STT 暂时不可用
   - `supabase.functions.invoke('match')` → 后端有实现但可能需调参
   - `supabase.from('device_registry').select('companion_model:companion_models(*, avatar:avatar_asset_id(*))')` → 嵌套查询不支持，需简化或后端新增路由

2. **管理页面（13个）**：仍通过 supabase.ts 兼容层导入，依赖通用查询路由

3. **前端登录页面**：需用户测试注册/登录流程是否正常

### 服务器信息

- IP: 47.98.103.151
- 前端: http://47.98.103.151/ (nginx → /var/www/iot-ai-doll/frontend/)
- 后端: http://47.98.103.151/api/ (nginx 反代 → localhost:3000)
- 后端进程: PM2 iot-backend (pid 16777)
- 后端代码: /var/www/iot-ai-doll/backend/src/

### 下一步

1. 用户测试登录注册流程
2. 完善 GamePage.tsx 中 supabase.functions.invoke 调用（TTS/STT 需接入真实 AI 服务）
3. 处理 GalleryPage.tsx 和 DeviceBindPage.tsx 的 supabase 调用
4. 完善管理页面的兼容层

### 23:53  -  Admin账号管理

- 当前登录系统只允许纯数字用户名，导致admin账号无法正常登录
- 手机号15622205445已被注册为普通用户，计划将其升级为admin账号
